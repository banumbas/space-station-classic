- type: quickConstructionCategory
  id: Glass
  constructionEntries:
    - SheetRGlass # SL
    - SheetUGlass # SL
    - SheetPGlass # SL
    - Window
    # Classic-Edit - diagonal window construction is intentionally omitted.
    - WindowDirectional

- type: quickConstructionCategory
  id: ReinforcedGlass
  constructionEntries:
    - SheetRUGlass1 # SL
    - SheetRPGlass1 # SL
    - ReinforcedWindow
    # Classic-Edit - diagonal window construction is intentionally omitted.
    - WindowReinforcedDirectional

- type: quickConstructionCategory
  id: UraniumGlass
  constructionEntries:
    - SheetRUGlass0 # SL
    # Classic-Start
    # Uranium windows are intentionally unavailable from quick construction.
    # Classic-End

- type: quickConstructionCategory
  id: ReinforcedUraniumGlass
  constructionEntries: [] # Classic-Edit - uranium windows are not constructible.

- type: quickConstructionCategory
  id: PlasmaGlass
  constructionEntries:
    - SheetRPGlass0 # SL
    - PlasmaWindow
    # Classic-Edit - diagonal window construction is intentionally omitted.
    - PlasmaWindowDirectional

- type: quickConstructionCategory
  id: ReinforcedPlasmaGlass
  constructionEntries:
    - ReinforcedPlasmaWindow
    # Classic-Edit - diagonal window construction is intentionally omitted.
    - PlasmaReinforcedWindowDirectional

- type: quickConstructionCategory
  id: ClockworkGlass
  constructionEntries:
    - ClockworkWindow
    # Classic-Edit - diagonal window construction is intentionally omitted.
    - WindowClockworkDirectional
